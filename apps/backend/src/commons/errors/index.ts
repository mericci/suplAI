import { isError } from './is-error';
import { isErrorWithReason } from './is-error-with-reason';
import { isNotFoundError } from './is-not-found-error';
import { isProfileNotFoundError } from './is-profile-not-found-error';

export {
  isError, isErrorWithReason, isNotFoundError, isProfileNotFoundError,
};
