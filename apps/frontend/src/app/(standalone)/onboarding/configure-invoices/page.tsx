'use client';

import { ConfigureInvoicesForm } from '@/features/onboarding/ConfigureInvoicesForm';

export default function ConfigureInvoicesPage(): React.JSX.Element {
  return <ConfigureInvoicesForm />;
}
